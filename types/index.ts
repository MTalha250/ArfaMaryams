type Product = {
  _id: string;
  name: string;
  price: number;
  images: string[];
  category: string;
  stock: number;
  description: string;
  createdAt: string;
  updatedAt: string;
};

export type { Product };
